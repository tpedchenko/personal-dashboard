# ── Virtual environments ──
venv/
test-gui/.venv/

# ── Python cache ──
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ── Secrets & credentials (NEVER commit!) ──
.env
.env.*
deploy/config.env
.streamlit/secrets.toml
test-gui/auth.json

# ── Database (lives on NAS, NOT in GitHub) ──
data/
data_backup_*/
import/

# ── LLM ──
ml-training/

# ── Personal/planning notes ──
4import/
2do-ai*.md
banks.md
db.md
documentation/

# ── IDE & tools ──
.vscode/
.idea/
.azure/
.claude/
.playwright-mcp/

# ── OS ──
.DS_Store
Thumbs.db

# ── Deploy examples (keep), secrets (exclude) ──
# deploy/config.env.example — tracked
# .streamlit/secrets.toml.example — tracked
deploy/personal-dash/config.env
.next/
ml-training/fused-model/
ml-training/*.csv
